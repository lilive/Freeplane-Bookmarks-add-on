// Create a link to this node.
// The node where the link is created is a bookmark, or the memorized node if it exists.

import bookmarks.CreateLinkGUI
CreateLinkGUI.show( node, CreateLinkGUI.Mode.IN )
